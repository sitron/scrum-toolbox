{% extends "SitronnierSmBoxBundle::layout.html.twig" %}

{% block body %}
<h1>New sprint</h1>

<form action="{{ path('sprint_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('sprint') }}">See all sprints</a>
    </li>
</ul>
{% endblock %}
